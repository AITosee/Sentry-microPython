from machine import I2C,UART,Pinfrom Sentry import *import time# 等待Sentry2完成操作系统的初始化。此等待时间不可去掉，避免出现Sentry2尚未初始化完毕主控器已经开发发送指令的情况time.sleep(2)# 选择UART或者I2C通讯模式，Sentry2需要进行相应的设置，出厂默认为I2C模式# 参看“设置通讯方式”# https://tosee.readthedocs.io/zh/latest/Sentry2/Hardware/index.html#section-3########################################################################################################## port = UART(2,rx=Pin(16),tx=Pin(17),baudrate=9600)port = I2C(1,scl=Pin(22),sda=Pin(21),freq=400000)# Sentry2默认通讯地址0x60，支持0x60-0x63四个地址。如果I2C总线挂接多个设备，请避免出现地址冲突sentry = Sentry2(0x60)err = sentry.begin(port)print("sentry.begin: 0x%x"% err) # 1、算法文档# https://tosee.readthedocs.io/zh/latest/Sentry2/Vision/index.html#chapter-vision-line-index# 2、算法视频# https://www.bilibili.com/video/BV1de4y137QU/?spm_id_from=333.1387.upload.video_card.click&vd_source=a6a4a13d17ed26ee248472534a8ec3cc# 3、请保持默认设置：只检测一条线段# 4、正常使用时，应由主控器发送指令控制Sentry2算法的开启与关闭，而非通过摇杆手动进行操作；# 5、满足特定限制条件下，Sentry2可以并行运行多个识别算法# 参看“算法列表”# https://tosee.readthedocs.io/zh/latest/Sentry2/Hardware/index.html#section-3# 6、如需Sentry2启动后，自行运行某种算法，可以在开启算法后，通过设置“寄存器”->“保存当前值”实现。# 参看“设置寄存器”err = sentry.VisionBegin(sentry2_vision_e.kVisionLine)print("sentry.VisionBegin(sentry2_vision_e.kVisionLine):0x%x"% err)def Go_Straight():  print("Go Straight")    def Left():  print("Left")  def Right():  print("Right")  def Hard_Left():  print("Hard Left")  def Hard_Right():  print("Hard Right")  def Stop():  print("Stop")        while True:    # Sentry2不主动返回检测识别结果，需要主控板发送指令进行读取。读取的流程：首先读取识别结果的数量，接收到指令后，Sentry2会刷新结果数据，如果结果数量不为零，那么主控再发送指令读取结果的相关信息。请务必按此流程构建程序。    obj_num = sentry.GetValue(sentry2_vision_e.kVisionLine, sentry_obj_info_e.kStatus)    if obj_num:        # 按照默认设置只巡一条线，所以只需要读取结果1的信息并进行处理即可        angle = (sentry.GetValue(sentry2_vision_e.kVisionLine,sentry_obj_info_e.kLabel,1))        print("angle= %d"%(angle))        if (45 > angle):          Hard_Right()        if ((80 > angle) and (angle > 45)):          Right()        if ((100 > angle) and (angle > 80)):          Go_Straight()        if ((135 > angle) and (angle > 100)):          Left()        if (angle > 135):          Hard_Left()    else:        Stop()            time.sleep(0.2)        print("\n")